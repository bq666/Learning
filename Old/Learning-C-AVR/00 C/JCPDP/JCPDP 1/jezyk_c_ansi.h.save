#ifndef JEZYK_C_ANSI_H_INCLUDED
#define JEZYK_C_ANSI_H_INCLUDED







/*     int vals[5] = {10,11,12,13,14};


        printf(" Pierwsza wartość w tablicy vals: %d\n\n", *vals);
        printf(" Pierwsza wartość w tablicy vals: %d\n\n", vals[0]);

        for (i = 0; i < 5; i++){
            printf("%d, \n\n\n\n", *(vals+i));
        }


        int i;
        char name[] = "Teodor Rumstyk";
        char *pname = "Pizda pizdowata";

        printf("%d\n\n", strlen(name));
        printf("%d\n\n", strlen(pname));

        for (i = 0; i < 20; i++)
            printf("%c", pname + i);
        printf("\n\n\n");

*/








/*
 printf("Niektóre granie liczb dla tego systemu:\n");
        printf("MAX int: \t\t%d\n", INT_MAX);
        printf("MIN int: \t\t%d\n", INT_MIN);
        printf("MAX long long: \t%lld\n", LONG_LONG_MAX);
        printf("MIN long long: \t%lld\n", LONG_LONG_MIN);
        printf("Jeden bajt = \t%d bitów\n", CHAR_BIT);
        printf("Float precyzja = %d cyfr\n", FLT_DIG);
       // printf("Bool = %d\n", )
*/



/*
#define PI 3.14159

        float powierzchnia, obwod, promien;
        printf("Ile wynosi promieñ Twojej pizzy: ");
        scanf(" %f", &promien);
        powierzchnia = PI * promien * promien;
        obwod = 2.0 * PI * promien;
        printf("Podstawowe parametry Twojej pizzy to:\n");
        printf("obwód = %1.2f, powierzchnia = %1.2f\n", obwod, powierzchnia);
        printf("obwód = %2.3f, powierzchnia = %1.2f\n", obwod, powierzchnia);
        printf("obwód = %3.4f, powierzchnia = %1.2f\n", obwod, powierzchnia);
        printf("obwód = %4.5f, powierzchnia = %1.2f\n", obwod, powierzchnia);
        printf("obwód = %5.6f, powierzchnia = %1.2f\n", obwod, powierzchnia);
        printf("obwód = %6.7f, powierzchnia = %1.2f\n", obwod, powierzchnia);
        printf("obwód = %7.8f, powierzchnia = %1.2f\n", obwod, powierzchnia);
        printf("obwód = %8.9f, powierzchnia = %1.2f\n", obwod, powierzchnia);
        printf("obwód = %015.10f, powierzchnia = %1.2f\n", obwod, powierzchnia);
        printf("obwód = %15.11f, powierzchnia = %1.2f\n", obwod, powierzchnia);
*/



/*
#define POCHWALA "Ach, jakie zajebiste imie!"

        char imie [40];
        printf("Jakie masz imie?");
        scanf(" %s", imie);
        printf("Witaj, %s. %s\n", imie, POCHWALA);
        printf("Twoje imiê, sk³adaj¹ce siê z %u ---> w C99,C11 %zd <--- liter, zajmuje %u komórek pamiêci.\n", strlen(imie), sizeof(imie));
        printf("POCHWALA sk³ada siê z %u liter ",strlen(POCHWALA));
        printf("i zajmuje %u komórek pamiêci", sizeof(POCHWALA));
*/



/*
#define POCHWALA "Ach, jakie zajebiste imie!"

        char imie [40];
        printf("Jakie masz imie?");
        scanf(" %s", imie);
        printf("Witaj, %s. %s\n", imie, POCHWALA);
*/



/*
#define GESTOSC 62.4
        float waga, objetosc;
        int rozmiar, litery;
        char nazwa[40];

        printf("Witam ! Podaj swoje imie. \n");
        scanf(" %s", nazwa);
        printf("%s, ile fintów wa¿ysz: ", nazwa);
        scanf(" %f", &waga);
        rozmiar = sizeof (nazwa);
        litery = strlen(nazwa);
        objetosc = waga / GESTOSC;
        printf("Zatem, %s, twoja objêtoœæ wynosi %2.2f stopy szeœcienne.\n", nazwa, objetosc);
        printf("Twoje imiê zawiera %d liter(y),\n", litery);
        printf("a na jego przechowanie potrzebujemy %d bajtów.\n", rozmiar);
*/

/*
  long nc;

     nc = 0;
     while (getchar() != EOF)
        ++nc;
     printf("%ld\n", nc);

     for (nc = 0;getchar() != EOF; ++nc)
        ;
     printf("%ld\n", nc);
*/



/*
   int c;
        c = getchar();
        while (c != 'x' ){        //<--- /*EOF to ctrl+z
            putchar(c);
            c = getchar();
        }

        while ((c = getchar()) != 'x' )    //<--- /*EOF to ctrl+z
            putchar(c);

        printf("EOF = %i\n", EOF);
        c = EOF;
        printf("%i\n", c);

        printf("%d\n", getchar() != EOF);
*/













#endif // JEZYK_C_ANSI_H_INCLUDED
